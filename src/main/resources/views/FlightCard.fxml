<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.geometry.Pos?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox xmlns="http://javafx.com/javafx/8"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="controllers.FlightCardController"
      style="-fx-border-color: #cccccc; -fx-border-width: 1; -fx-border-radius: 5; -fx-padding: 5; -fx-background-color: #ffffff;"
      alignment="CENTER">

    <!-- Add spacing and padding to the VBox -->
    <padding>
        <Insets top="5" right="5" bottom="5" left="5"/>
    </padding>
    <spacing>5</spacing>

    <!-- Flight Time and Duration -->
    <HBox spacing="10" alignment="CENTER">
        <!-- Departure Time -->
        <Label fx:id="departureTimeLabel" style="-fx-font-size: 14px; -fx-font-weight: bold;" />
        <Label text="→" style="-fx-font-size: 14px; -fx-font-weight: bold;" />
        <!-- Arrival Time -->
        <Label fx:id="arrivalTimeLabel" style="-fx-font-size: 14px; -fx-font-weight: bold;" />
        <!-- Duration -->
        <Label fx:id="durationLabel" style="-fx-font-size: 12px; -fx-text-fill: #666666;" />
    </HBox>

    <!-- Flight Details -->
    <HBox spacing="10" alignment="CENTER">
        <!-- Departure Airport -->
        <Label fx:id="departureAirportLabel" style="-fx-font-size: 12px; -fx-text-fill: #333333;" />
        <Label text="Direct" style="-fx-font-size: 12px; -fx-text-fill: #666666;" />
        <!-- Arrival Airport -->
        <Label fx:id="arrivalAirportLabel" style="-fx-font-size: 12px; -fx-text-fill: #333333;" />
    </HBox>

    <!-- Price and Offers -->
    <HBox spacing="10" alignment="CENTER_RIGHT">
        <!-- Price -->
        <Label fx:id="priceLabel" style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #007bff;" />
        <Label text="2 offres clés" style="-fx-font-size: 12px; -fx-text-fill: #666666;" />
    </HBox>

    <!-- View Details Button -->
    <HBox alignment="CENTER">
        <!-- Added fx:id for the button so it can be accessed in the controller -->
        <Button fx:id="viewDetailsButton"
                text="View Details"
                onAction="#handleViewDetails"
                style="-fx-background-color: #007bff; -fx-text-fill: white; -fx-font-size: 14px; -fx-padding: 5px 15px; -fx-border-radius: 5;" />
    </HBox>
</VBox>